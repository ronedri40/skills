# skill-name

One-line description.

**Invoke:** `/skill-name` or `/skill-name <argument>`

**Tools used:** Bash, Read, ...

## What it does

Short description of the skill's behaviour.

## Requirements

Any prerequisites (CLI tools, env vars, directory context).
